#ifndef PRINTER_H
#define PRINTER_H
#include <iostream>

/**
 * @brief This namespace is created so that we can create a libary from it 
 * that we can import into an executable
 */
namespace JamesStuff{

    void Printer(std::string textToPrint);
}

#endif